# ARCHIVED — stop

This repo is **archived**. Read [`ARCHIVED.md`](./ARCHIVED.md), then leave.

The live DreamChat frontend is **`dream-weaver-visuals`** (dev port **5273**); its `AGENTS.md` is the
one you want. Anything crossing a repo boundary is governed by `../AGENTS.md` and
`../docs/00_workspace/`. Authority: `workspace:ADR-W003`.

Do no work here. Do not fix it, build it, deploy it, or depend on it. Port 5173 is retired with it.

Two claims the previous version of this file made, retracted, because you may have read them:

- It called itself "the canonical instruction set for *any* coding agent working in this repo." **It
  is not.** That is `dream-weaver-visuals/AGENTS.md` for frontend work, and `../AGENTS.md` for the
  workspace.
- It said "There is no `/docs` tree here." **There are two** — `docs/20_design_ux/` and
  `docs/superpowers/` — and they are the reason this repo is kept: provenance, not authority.
